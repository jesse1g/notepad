{
  "name": "server",
  "version": "1.0.0",
  "description": "notepad api server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "test": "jest"
  },
  "author": "Jesse Guo",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.0",
    "body-parser": "^1.17.2",
    "cors": "^2.8.4",
    "express": "^4.15.4",
    "morgan": "^1.8.2",
    "nodemon": "^1.11.0",
    "pg-promise": "^6.5.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "eslint": "^4.5.0",
    "jest": "^20.0.4",
    "superagent": "^3.6.0",
    "supertest": "^3.0.0"
  }
}
